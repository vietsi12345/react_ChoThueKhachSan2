const initstate = {
    isLoggerIn: false,
    token: null
}

const authReducer = (state = initstate, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default authReducer